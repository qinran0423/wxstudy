<view class="wrapper">
  <!-- <view>这里是组件的内部节点</view>
  <button class="btn" bindtap="doSometing" >Do something</button>
  <slot name="top"></slot>
  <view>两个slot</view>
  <slot name="bottom"></slot>
  <text class="red-text ~blue-text">这段文本的颜色由 `app.wxss` 和页面 `wxss` 中的样式定义来决定</text> -->
  蛮宽的
  <slot></slot>

</view>